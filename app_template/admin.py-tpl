from django.contrib import admin

from melodramatick.work.admin import BaseWorkAdmin
from .models import {{ work|title }}

@admin.register({{ work|title }})
class {{ work|title }}Admin(BaseWorkAdmin):
    base_model = {{ work|title }}
