import django_tables2 as tables
from melodramatick.work.tables import WorkTable

from .models import {{ work|title }}

{{ work|title }}Table = tables.table_factory({{ work|title }}, table=WorkTable, fields=["position", "title", "composer",])
